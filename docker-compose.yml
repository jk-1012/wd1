version: "3.8"

services:
  web:
    build: .
    container_name: travel_booking_web
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"  # Ports must be a list
    environment:
      - DEBUG=True
      - DATABASE_URL=sqlite:///db.sqlite3